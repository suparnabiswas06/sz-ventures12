# Vendored library source clones — reference only, not part of the site.
# The actual library files live in assets/js/libs and assets/styles/libs.
vendor-aos/
vendor-lenis/
vendor-tippy/
vendor-odometer/
vendor-swup/

# Unrelated Claude plugin repo (separate git project).
ui-ux-pro-max-skill/

# ── Deployment / tooling ──────────────────────────────────────────────────────
.vercel/          # Vercel project link (generated by `vercel link`)
node_modules/     # npm packages (if any tooling is added)
.env              # environment secrets — NEVER commit
.env.local
.env.production
.env.staging

# ── OS / Editor ──────────────────────────────────────────────────────────────
.DS_Store
Thumbs.db
desktop.ini
.idea/
.vscode/settings.json
*.swp
*.swo

.vercel
